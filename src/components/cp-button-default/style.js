import { makeStyles } from "@material-ui/core/styles";
import { COLORS } from "../../styles/colors";

const useStyles = makeStyles(() => ({
  buttonDefault__btn_global: {
    width: "100%",
    backgroundColor: COLORS.BTN_COLOR,
  },
}));

export default useStyles;
